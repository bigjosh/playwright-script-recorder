# Python
__pycache__/
*.pyc

# Recorder artifacts: capture baselines, diff dumps and replay logs live
# next to the scripts that use them; commit deliberately if a script is
# shared.
capture-*.png
compare-diff-*.png
*.log
screenshots/
